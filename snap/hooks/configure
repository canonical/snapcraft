#! /bin/bash

config_file="$SNAP_DATA/snap-config.yaml"
provider="$(snapctl get provider)"

cat <<EOT > "${config_file}"
# This file is automatically generated by snapcraft's configure hook.
# Do not write directly to this file. Instead, call 'snap set snapcraft <key>=<value>'.

"provider": "${provider}"
EOT
