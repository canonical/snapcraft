module: autotools_project
build-packages: [autoconf, automake, autopoint]
options:
  source:
    required: true
  source-type:
  source-tag:
  source-branch:
  configflags:
    required: false
  stage-packages:
  snap:
  stage:
