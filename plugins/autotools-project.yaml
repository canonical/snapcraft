module: autotools_project
build-tools: [autoconf, automake, autopoint]
options:
    source:
        required: true
    source-type:
    source-tag:
    source-branch:
    configflags:
        required: false
