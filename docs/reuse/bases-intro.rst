Snaps declare a base in their project file. The base defines the feature set used by
`Snapcraft`_, the ``snapcraft.yaml`` schema, the environment where the snap is built,
and which base snap is used at runtime.
