name: pypi-config
version: 1
vendor: Vendor <email@example.com>
summary: Create a snappy config hook using setup.py
description: |
 The object is to setup a snappy config hook to setup configuration based
 on python code.

 This package will download its dependencies from pypi.
icon: icon.png
config: usr/bin/config.py

parts:
  config:
    type: python3-project
    source: .
