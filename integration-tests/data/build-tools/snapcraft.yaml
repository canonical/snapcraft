name: build-tools-test
version: 0.1
summary: one line summary
description: a longer description
icon: icon.svg

parts:
  make-project:
    plugin: make
    source: .
    stage-packages:
      - libxml2-dev
      - libxslt1-dev
      - libglib2.0-dev
