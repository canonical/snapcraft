name: pip-test
version: 1
vendor: PIP Tests <pip@pippy.pi>
summary: Why do snapcraft files require a summary?
description: This seems a bit excessive too
icon: icon.svg

parts:
  python2:
    plugin: python2-project
    requirements: requirements.txt
  python3:
    plugin: python3-project
    requirements: requirements.txt
