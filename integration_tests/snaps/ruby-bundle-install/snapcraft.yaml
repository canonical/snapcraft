name: ruby-bundle-install
version: 0.1
summary: Test bundle install
description: |
  Snap to test 'bundle install' command
confinement: strict

parts:
  ruby-bundle-install:
    plugin: ruby
    use-bundler: true
    ruby-version: "2.3.1"
    stage:
      - bin
