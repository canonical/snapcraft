name: build-package-version
version: '0.1'
summary: install a specific, but non-existent, version of a build-package
description: |
  Install a specific, but non-existent version of a build-package.

grade: stable
confinement: strict

parts:
  hello:
    plugin: nil
    build-packages: ['hello=x.y-z']
