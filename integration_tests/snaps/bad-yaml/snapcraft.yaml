name: bad-yaml
version: '0.1'
summary: This is an error case snap
description: |
    This has bad yaml and should fail to load. There is a tab indenting the
    `plugin` line.

grade: devel
confinement: devmode # use 'strict' once you have the right plugs and slots

parts:
  my-part:
	plugin: nil
