name: test-package
version: 0.1
summary: A test of the copy plugin with dest
description: |
    Copies all files present in local source to destination subdir. The test verifies they're there.
icon: icon.png

parts:
  copy-example:
    plugin: copy
    destination: subdir
