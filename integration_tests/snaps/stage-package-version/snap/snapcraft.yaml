name: stage-package-version
version: '0.1'
summary: install a specific, but non-existent version of a stage-package
description: |
  Install a specifi, but non-existent  version of  a stage-package

grade: stable
confinement: strict

parts:
  hello:
    plugin: nil
    stage-packages: ['hello=x.y-z']
