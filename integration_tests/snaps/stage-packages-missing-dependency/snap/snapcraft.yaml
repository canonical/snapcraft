name: stage-packages-missing-dependency
version: '0.1'
summary: install a stage package
description: |
  Install a stage package.
  This package has one dependency: gcc-6-base.

grade: stable
confinement: strict

parts:
  part-with-stage-packages:
    plugin: nil
    stage-packages: ['hello']
