name: simple-gulp
version: 0
summary: This is an integration test for the gulp plugin
description: |
    If this passes we will know that the gulp plugin is behaving correctly
confinement: strict

parts:
    gulp:
        plugin: gulp
        gulp-tasks: ['install']
        source: .
