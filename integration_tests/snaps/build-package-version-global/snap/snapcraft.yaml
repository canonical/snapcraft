name: build-package-version
version: '0.1'
summary: install a specific version of a global build-package
description: |
  Install a specific version of a global build-package.

grade: stable
confinement: strict
build-packages: ['hello=2.10-1']

parts:
  build-package-version-global:
    plugin: nil
