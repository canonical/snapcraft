name: test-package
version: 0.1
summary: test a simple scons project
description: a longer description
icon: icon.png

build-packages: [gcc, libc6-dev]

parts:
  scons-project:
    scons-options:
      - --prefix=/opt
    plugin: scons
    source: .
