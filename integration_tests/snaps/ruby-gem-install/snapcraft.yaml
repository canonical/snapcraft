name: ruby-gem-install
version: 0.1
summary: Test gem install
description: |
  Snap to test 'gem install <gem>' command
confinement: strict

parts:
  ruby-gem-install:
    plugin: ruby
    ruby-version: '2.4.2'
    gems: ['rack']
    stage:
      - bin
