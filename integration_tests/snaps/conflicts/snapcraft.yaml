name: test-package
version: 0.1
summary: 
description: a longer description
icon: icon.png
confinement: strict

build-packages: [gcc, libc6-dev]

parts:
  conflicts-p1:
    plugin: make
    source: .
  conflicts-p2:
    plugin: make
    source: ./p2
    after: [conflicts-p1]
