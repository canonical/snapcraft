name: build-package-version
version: '0.1'
summary: install a specific version of a build-package
description: |
  Install a specific version of a build-package.

grade: stable
confinement: strict

parts:
  hello:
    plugin: nil
    build-packages: ['hello=2.10-1']
