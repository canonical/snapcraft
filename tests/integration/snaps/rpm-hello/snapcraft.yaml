name: rpm-hello
version: '0.1'
summary: test the rpm source
description: |
  This is a basic rpm snap. It just prints a hello world.
  This is a basic autotools snap. It just prints a hello world.
  Make a new one.

grade: devel
confinement: devmode

parts:
  rpm:
    plugin: dump
    source: small-0.1-1.noarch.rpm
