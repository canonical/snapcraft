name: nil-with-additional-properties
version: '1.0'
summary: Use additional property with nil
description: Use additional property with nil (which should error out)

grade: devel
confinement: strict

parts:
  nil-part:
    plugin: nil
    extra-property: foo
