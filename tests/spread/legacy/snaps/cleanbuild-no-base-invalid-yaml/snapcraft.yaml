  name: cleanbuild-no-base-invalid-yaml
version: "0.1"
summary: cleanbuild backwards compatibility for invalid yaml when no base is set
description: >
  Ensure that we invalid yaml is handled correctly when cleanbuild is used
  without a base.

grade: devel
confinement: devmode

parts:
  test:
    source:
