name: cleanbuild-with-base-valid-yaml
base: core18
version: "0.1"
summary: cleanbuild failure for valid yaml when a base is set
description: >
  Ensure that we exit correctly with a notice that cleanbuild is no
  longer supported in snapcraft when using bases.
  
grade: devel
confinement: devmode

parts:
  test:
    plugin: nil
