  name: cleanbuild-with-base-invalid-yaml
base: core18
version: "0.1"
summary: cleanbuild backwards compatibility for invalid yaml when a base is set
description: >
  Ensure that we invalid yaml is handled correctly when cleanbuild is used
  with a base.
  
grade: devel
confinement: devmode

parts:
  test:
    source:
