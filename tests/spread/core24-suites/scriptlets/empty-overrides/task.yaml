summary: Execute empty scriptlets

restore: |
  snapcraft clean
  rm -rf squashfs-root

execute: |
  snapcraft pack
  unsquashfs test-snap_0.1_amd64.snap

  if [ -f squashfs-root/foo.bar ]; then
      echo "prime directory should've been empty"
      false
  fi
