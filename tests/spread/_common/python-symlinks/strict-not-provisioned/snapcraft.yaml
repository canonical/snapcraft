name: strict-not-provisioned
version: "1.0"
summary: Strict confinement without staged python
description: The core26 base doesn't include Python, so the build must fail.

grade: devel
base: core26
build-base: devel
confinement: strict

apps:
  hello:
    command: bin/hello

parts:
  python-sample:
    plugin: python
    source: src
    python-packages: [black]
