name: git-commit
base: core20
version: "0.1"
summary: one line summary
description: a longer description
confinement: strict

parts:
    git:
        plugin: dump
        source: https://github.com/snapcore/core18
        source-type: git
        source-commit: "HEAD"
