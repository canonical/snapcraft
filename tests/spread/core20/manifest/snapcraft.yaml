name: manifest
base: core20
version: '0.1'
summary: test for manifests
description: test for manifests

grade: devel
confinement: devmode

parts:
  my-part:
    plugin: nil
    stage-packages:
      - hello
  other-part:
    plugin: nil
