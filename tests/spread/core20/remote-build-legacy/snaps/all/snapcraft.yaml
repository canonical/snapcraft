name: test-snap-all-core20
base: core20
version: "1.0"
summary: Test snap for remote build
description: Test snap for remote build

grade: stable
confinement: strict

architectures:
  - build-on: [amd64]
    run-on: [all]

parts:
  my-part:
    plugin: nil
