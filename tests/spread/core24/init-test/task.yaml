summary: Init and execute tests

prepare: |
  rm -rf test-snap

restore: |
  rm -rf test-snap
  rm -rf ./*.snap

execute: |
  mkdir test-snap
  cd test-snap
  snapcraft init
  snapcraft init --profile=test
  snapcraft test
