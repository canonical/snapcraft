name: build-for-all
version: "1.0"
summary: test
description: |
  Test building of `build-for: all`
grade: devel
confinement: strict
base: core24
build-base: devel
architectures:
  - build-on: amd64
    build-for: all

parts:
  nil:
    plugin: nil
