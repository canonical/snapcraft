name: single-arch
version: "1.0"
summary: test
description: |
  A single architecture should create a single snap file.
grade: devel
confinement: strict
base: core24
build-base: devel
architectures:
  - build-on: amd64
    build-for: amd64

parts:
  nil:
    plugin: nil
