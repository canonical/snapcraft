summary: Build and run a basic GPU snap using extensions

environment:
  SNAP_DIR: ../snaps/gpu-hello

prepare: |
  #shellcheck source=tests/spread/tools/snapcraft-yaml.sh
  . "$TOOLS_DIR/snapcraft-yaml.sh"
  set_base "$SNAP_DIR/snap/snapcraft.yaml"

  # remove this once mesa-2604 is available on stable (#6238)
  if [[ "$SPREAD_SYSTEM" =~ ubuntu-26.04 ]]; then
    snap install mesa-2604 --edge
  fi

execute: |
  # This test is only intended to run on amd64, where the content snap is available
  [ "$(dpkg --print-architecture)" = "amd64" ] || exit 0

  cd "$SNAP_DIR"

  snapcraft pack | NOMATCH "GPU support library should be provided by a content interface"

  snap install gpu-hello_*.snap --dangerous

  gpu-hello.gpuinfo

restore: |
  cd "$SNAP_DIR"
  snapcraft clean
  rm -f ./*.snap

  #shellcheck source=tests/spread/tools/snapcraft-yaml.sh
  . "$TOOLS_DIR/snapcraft-yaml.sh"
  restore_yaml "snap/snapcraft.yaml"
