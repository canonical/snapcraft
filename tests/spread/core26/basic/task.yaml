summary: Build a snap using the 'core26' base

environment:
  COMMAND/default: ""
  COMMAND/pack: "pack"

restore: |
  snapcraft clean
  rm -f ./*.snap

execute: |
  # shellcheck disable=SC2086
  snapcraft $COMMAND
