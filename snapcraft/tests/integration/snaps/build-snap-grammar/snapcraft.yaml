name: build-snap-grammar
version: '0.1'
summary: Test the build snap grammar
description: Simple, standalone build-snap
grade: devel
confinement: strict

parts:
  my-part:
    plugin: nil
    build-snaps:
      - hello
