name: test-environment
version: 0.1
summary: test the SNAPCRAFT_.* variables
description: tests the snapcraft exported variables inside a project
confinement: strict

parts:
  env:
    plugin: make
    source: .
