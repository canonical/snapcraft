name: test-git-depth
version: '0.1'
summary: Test the use of source-depth
description: Make sure source-depth works
confinement: strict

parts:
    git:
        plugin: make
        source: .
        source-type: git
        source-depth: 1
