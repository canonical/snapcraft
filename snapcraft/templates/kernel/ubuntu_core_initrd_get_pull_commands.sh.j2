# Ubuntu core initrd snapcraft pull sources script
# Generated by snapcraft ubuntu-core-initrd plugin# ----
env


{% if is_cross_compiling %}
    echo \
        "deb [arch={{ target_arch }}] http://ports.ubuntu.com/ubuntu-ports \
    {{ ubuntu_kernel_release_name }} main restricted universe multiverse" >> /etc/apt/sources.list.d/{{ ubuntu_kernel_release_name }}_ports.list
    sed- i 's/Architecture: amd64/Architecture: amd64 arm64/g' /etc/apt/sources.list.d/*snappy*.list
    apt-get update

    apt-get --download $(apt-rdepends busybox-static:{{ target_arch }} | grep -v "^ "
{% endif %}
